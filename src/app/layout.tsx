import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Universal Time Converter - Convert Any Date/Time Format",
  description: "Easily convert between different date and time formats including ISO 8601, Unix timestamps, natural language, and more. Supports multiple parsing libraries and timezone conversions.",
  keywords: "time converter, date converter, timezone, Unix timestamp, ISO 8601, date format, time format",
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
